{
  "name": "nasa-space-bioscience-explorer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd ui && npm install && npm run build",
    "dev": "cd ui && npm run dev",
    "start": "cd ui && npm run start"
  },
  "engines": {
    "node": ">=18.18.0",
    "npm": ">=9.0.0"
  }
}
